---
- name: Uninstall Knative Serving
  hosts: localhost
  vars:
    knative_version: v0.25.0

  tasks:
    - name: Uninstall Knative Kourier
      command: |
        kubectl delete -f https://github.com/knative/net-kourier/releases/download/"{{ knative_version }}"/kourier.yaml

    - name: Uninstall Knative Serving CRDS
      command: |
        kubectl delete -f https://github.com/knative/serving/releases/download/"{{ knative_version }}"/serving-crds.yaml

    - name: Uninstall Knative Serving Core
      command: |
        kubectl delete --ignore-not-found=true -f https://github.com/knative/serving/releases/download/"{{ knative_version }}"/serving-core.yaml